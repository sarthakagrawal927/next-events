import { useState, useEffect } from "react";
import axios from "axios";

function PythonTest(props) {
  // const [incoming, setIncoming] = useState();

  // useEffect(() => {
  //   axios.get("/api/pytest").then((res) => {
  //     setIncoming(res.data);
  //   });
  // }, [incoming]);

  return <p>sad</p>;
}

export default PythonTest;
